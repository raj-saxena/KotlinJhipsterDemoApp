apply plugin: "war"

bootWar {
    mainClassName = "com.tbst.kjd.KotlinSpringMonolithApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
